import { Link, useForm } from '@inertiajs/react';
import type { FormEvent } from 'react';
import AppLayout from '../../layouts/AppLayout';

type LeaveType = { id: number; code: string; name: string; tracks_balance: boolean; balance?: string | null; entitlement_label?: string | null };
type LeaveRequest = { id: number; start_date: string; end_date: string; units: string; status: string; leave_type: LeaveType };
type Attendance = { id: number; occurred_at: string; event_type: string; source: string };

export default function Dashboard({ employee, department, leaveTypes, requests, attendance, canAdmin }: any) {
    const form = useForm({ leave_type_id: leaveTypes[0]?.id ?? '', start_date: '', end_date: '', units: 1, reason: '' });
    const submit = (e: FormEvent) => { e.preventDefault(); form.post('/hris/leave-requests', { preserveScroll: true, onSuccess: () => form.reset('start_date', 'end_date', 'reason') }); };
    return <AppLayout title="HRIS"><div className="mx-auto max-w-7xl space-y-6"><div className="flex flex-col gap-4 sm:flex-row sm:items-end sm:justify-between"><div><div className="text-xs font-bold uppercase tracking-[0.18em] text-blue-700">Employee self-service</div><h1 className="mt-2 text-3xl font-bold text-slate-950">My HR Record</h1><p className="mt-2 text-sm text-slate-500">{employee.employee_number} · {employee.position_title} · {department.name}</p></div>{canAdmin && <Link href="/hris/admin" className="rounded-xl bg-[#0b2852] px-4 py-3 text-sm font-semibold text-white">Open HR Administration</Link>}</div><section className="grid gap-4 sm:grid-cols-2 lg:grid-cols-5">{leaveTypes.map((type: LeaveType) => <div key={type.id} className="rounded-2xl border border-slate-200 bg-white p-5 shadow-sm"><div className="text-xs font-bold uppercase tracking-wide text-slate-500">{type.code}</div><div className="mt-2 text-sm font-semibold text-slate-900">{type.name}</div><div className="mt-4 text-2xl font-bold text-blue-900">{type.tracks_balance ? `${Number(type.balance || 0).toFixed(3)}` : (type.entitlement_label || 'Policy-based')}</div><div className="mt-1 text-xs text-slate-400">{type.tracks_balance ? 'available credits' : 'entitlement'}</div></div>)}</section><section className="grid gap-6 xl:grid-cols-[1fr_.9fr]"><form onSubmit={submit} className="space-y-5 rounded-3xl border border-slate-200 bg-white p-6 shadow-sm"><div><h2 className="font-bold text-slate-950">Apply for leave</h2><p className="mt-1 text-sm text-slate-500">Requested units are explicit in the prototype and remain subject to HR verification.</p></div><label className="block"><span className="mb-2 block text-sm font-semibold">Leave type</span><select value={form.data.leave_type_id} onChange={(e) => form.setData('leave_type_id', Number(e.target.value))} className="w-full rounded-xl border border-slate-300 px-4 py-3">{leaveTypes.map((type: LeaveType) => <option key={type.id} value={type.id}>{type.name}</option>)}</select></label><div className="grid gap-4 sm:grid-cols-2"><label><span className="mb-2 block text-sm font-semibold">From</span><input type="date" value={form.data.start_date} onChange={(e) => form.setData('start_date', e.target.value)} className="w-full rounded-xl border border-slate-300 px-4 py-3" /></label><label><span className="mb-2 block text-sm font-semibold">To</span><input type="date" value={form.data.end_date} onChange={(e) => form.setData('end_date', e.target.value)} className="w-full rounded-xl border border-slate-300 px-4 py-3" /></label></div><label className="block"><span className="mb-2 block text-sm font-semibold">Leave units</span><input type="number" step="0.5" min="0.5" value={form.data.units} onChange={(e) => form.setData('units', Number(e.target.value))} className="w-full rounded-xl border border-slate-300 px-4 py-3" /></label><label className="block"><span className="mb-2 block text-sm font-semibold">Reason</span><textarea rows={3} value={form.data.reason} onChange={(e) => form.setData('reason', e.target.value)} className="w-full rounded-xl border border-slate-300 px-4 py-3" /></label><button disabled={form.processing} className="rounded-xl bg-[#0b2852] px-5 py-3 text-sm font-semibold text-white">Submit leave request</button></form><div className="space-y-6"><div className="rounded-3xl border border-slate-200 bg-white p-6 shadow-sm"><h2 className="font-bold text-slate-950">Recent leave requests</h2><div className="mt-4 divide-y divide-slate-100">{requests.map((r: LeaveRequest) => <div key={r.id} className="py-3"><div className="flex justify-between gap-4"><div><div className="text-sm font-semibold text-slate-900">{r.leave_type.name}</div><div className="mt-1 text-xs text-slate-500">{new Date(r.start_date).toLocaleDateString()} – {new Date(r.end_date).toLocaleDateString()} · {Number(r.units).toFixed(1)} units</div></div><span className="h-fit rounded-full bg-slate-100 px-2.5 py-1 text-[10px] font-bold uppercase text-slate-700">{r.status}</span></div></div>)}{requests.length === 0 && <div className="py-5 text-sm text-slate-500">No leave requests yet.</div>}</div></div><div className="rounded-3xl border border-slate-200 bg-white p-6 shadow-sm"><h2 className="font-bold text-slate-950">Attendance activity</h2><div className="mt-4 space-y-3">{attendance.map((a: Attendance) => <div key={a.id} className="flex items-center justify-between rounded-xl bg-slate-50 px-4 py-3"><div><div className="text-sm font-semibold uppercase text-slate-800">{a.event_type}</div><div className="text-xs text-slate-500">{a.source}</div></div><div className="text-sm font-semibold text-slate-700">{new Date(a.occurred_at).toLocaleString()}</div></div>)}</div></div></div></section></div></AppLayout>;
}
