import { Building2, Crown, Gavel, Users } from 'lucide-react';
import AppLayout from '../../layouts/AppLayout';

type Office = {
    id: number;
    code: string;
    name: string;
    short_name?: string | null;
    active_employees_count: number;
    active_transactions_count: number;
    is_executive: boolean;
    is_legislative: boolean;
};

export default function Index({ departments, summary }: { departments: Office[]; summary: { offices: number; employees: number; activeTransactions: number } }) {
    return <AppLayout title="Departments"><div className="mx-auto max-w-7xl space-y-6"><div><div className="text-xs font-bold uppercase tracking-[0.18em] text-blue-700">Municipal organization</div><h1 className="mt-2 text-3xl font-bold text-slate-950">Department & Office Directory</h1><p className="mt-2 max-w-2xl text-sm leading-6 text-slate-500">Each employee account is anchored to an office. Department membership controls workspace context and contributes to server-side authorization decisions.</p></div><section className="grid gap-4 sm:grid-cols-3"><div className="rounded-2xl border border-slate-200 bg-white p-5 shadow-sm"><Building2 className="text-blue-800" size={20} /><div className="mt-4 text-3xl font-bold text-slate-950">{summary.offices}</div><div className="text-sm text-slate-500">configured offices</div></div><div className="rounded-2xl border border-slate-200 bg-white p-5 shadow-sm"><Users className="text-blue-800" size={20} /><div className="mt-4 text-3xl font-bold text-slate-950">{summary.employees}</div><div className="text-sm text-slate-500">active prototype employees</div></div><div className="rounded-2xl border border-slate-200 bg-white p-5 shadow-sm"><Building2 className="text-blue-800" size={20} /><div className="mt-4 text-3xl font-bold text-slate-950">{summary.activeTransactions}</div><div className="text-sm text-slate-500">active items across offices</div></div></section><section className="grid gap-4 md:grid-cols-2 xl:grid-cols-3">{departments.map((office) => <article key={office.id} className={`rounded-3xl border bg-white p-6 shadow-sm ${office.is_executive ? 'border-blue-200 ring-1 ring-blue-100' : 'border-slate-200'}`}><div className="flex items-start justify-between gap-4"><div className={`flex h-11 w-11 items-center justify-center rounded-2xl ${office.is_executive ? 'bg-blue-900 text-white' : office.is_legislative ? 'bg-indigo-50 text-indigo-800' : 'bg-slate-100 text-slate-700'}`}>{office.is_executive ? <Crown size={20} /> : office.is_legislative ? <Gavel size={20} /> : <Building2 size={20} />}</div><span className="rounded-full bg-slate-100 px-2.5 py-1 text-[10px] font-bold uppercase tracking-wide text-slate-600">{office.code}</span></div><h2 className="mt-5 text-lg font-bold leading-6 text-slate-950">{office.name}</h2><div className="mt-5 grid grid-cols-2 gap-3"><div className="rounded-xl bg-slate-50 p-3"><div className="text-xl font-bold text-slate-950">{office.active_employees_count}</div><div className="text-xs text-slate-500">Employees</div></div><div className="rounded-xl bg-slate-50 p-3"><div className="text-xl font-bold text-slate-950">{office.active_transactions_count}</div><div className="text-xs text-slate-500">Active Work</div></div></div>{office.is_executive && <div className="mt-4 rounded-xl bg-blue-50 px-3 py-2 text-xs font-semibold text-blue-800">Executive review and approval workspace</div>}{office.is_legislative && <div className="mt-4 rounded-xl bg-indigo-50 px-3 py-2 text-xs font-semibold text-indigo-800">Legislative records authority</div>}</article>)}</section><div className="rounded-2xl border border-amber-200 bg-amber-50 p-4 text-sm leading-6 text-amber-900"><strong>Prototype organization:</strong> the offices shown here are representative seed configuration. Production onboarding must replace or extend them using LGU Talibon's official organizational structure and authorized role assignments.</div></div></AppLayout>;
}
