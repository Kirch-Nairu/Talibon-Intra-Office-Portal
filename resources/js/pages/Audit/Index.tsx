import AppLayout from '../../layouts/AppLayout';

export default function Index({ events }: any) {
    return <AppLayout title="Audit & Security"><div className="mx-auto max-w-7xl space-y-6"><div><div className="text-xs font-bold uppercase tracking-[0.18em] text-blue-700">Security evidence</div><h1 className="mt-2 text-3xl font-bold text-slate-950">Audit & Security Events</h1><p className="mt-2 text-sm text-slate-500">Important workflow activity and denied access attempts recorded by the application.</p></div><div className="overflow-hidden rounded-3xl border border-slate-200 bg-white shadow-sm"><div className="hidden grid-cols-[170px_170px_1fr_110px_190px] gap-4 border-b border-slate-100 bg-slate-50 px-6 py-3 text-xs font-bold uppercase text-slate-500 md:grid"><div>User</div><div>Department</div><div>Event</div><div>Outcome</div><div>Time</div></div><div className="divide-y divide-slate-100">{events.map((event: any) => <div key={event.id} className="grid gap-2 px-6 py-5 md:grid-cols-[170px_170px_1fr_110px_190px] md:gap-4"><div className="text-sm font-semibold text-slate-900">{event.actor?.name || 'System'}</div><div className="text-sm text-slate-500">{event.actor?.employee?.department?.short_name || event.actor_department?.short_name || '—'}</div><div><div className="text-xs font-bold uppercase tracking-wide text-blue-700">{event.action}</div><div className="mt-1 text-sm text-slate-600">{event.summary}</div></div><div><span className={`rounded-full px-2.5 py-1 text-[10px] font-bold uppercase ${event.outcome === 'denied' ? 'bg-rose-50 text-rose-700' : 'bg-emerald-50 text-emerald-700'}`}>{event.outcome}</span></div><div className="text-xs text-slate-500">{new Date(event.created_at).toLocaleString()}</div></div>)}</div></div></div></AppLayout>;
}
