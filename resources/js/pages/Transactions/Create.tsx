import { Link, useForm } from '@inertiajs/react';
import type { FormEvent } from 'react';
import AppLayout from '../../layouts/AppLayout';

type Department = { id: number; code: string; name: string; short_name?: string };

export default function Create({ departments }: { departments: Department[] }) {
    const form = useForm({ transaction_type: 'funding_request', title: '', description: '', priority: 'normal', target_department_id: departments[0]?.id ?? '', remarks: '' });
    function submit(e: FormEvent) { e.preventDefault(); form.post('/transactions'); }

    return <AppLayout title="New Transaction"><div className="mx-auto max-w-3xl"><div className="mb-6"><Link href="/transactions" className="text-sm font-semibold text-blue-700">← Back to My Work</Link><h1 className="mt-3 text-3xl font-bold text-slate-950">Create and route transaction</h1><p className="mt-2 text-sm leading-6 text-slate-500">The origin office is taken from your authenticated employee profile. Submission creates the first immutable routing event.</p></div><form onSubmit={submit} className="space-y-6 rounded-3xl border border-slate-200 bg-white p-6 shadow-sm md:p-8"><div className="grid gap-5 md:grid-cols-2"><label><span className="mb-2 block text-sm font-semibold text-slate-700">Transaction type</span><select value={form.data.transaction_type} onChange={(e) => form.setData('transaction_type', e.target.value)} className="w-full rounded-xl border border-slate-300 px-4 py-3"><option value="funding_request">Funding Request</option><option value="project_endorsement">Project Endorsement</option><option value="document_review">Document Review</option><option value="internal_request">Internal Request</option><option value="other">Other</option></select></label><label><span className="mb-2 block text-sm font-semibold text-slate-700">Priority</span><select value={form.data.priority} onChange={(e) => form.setData('priority', e.target.value)} className="w-full rounded-xl border border-slate-300 px-4 py-3"><option value="normal">Normal</option><option value="high">High</option><option value="urgent">Urgent</option></select></label></div><label className="block"><span className="mb-2 block text-sm font-semibold text-slate-700">Title</span><input value={form.data.title} onChange={(e) => form.setData('title', e.target.value)} className="w-full rounded-xl border border-slate-300 px-4 py-3" placeholder="e.g. Road Rehabilitation Funding Request" />{form.errors.title && <span className="mt-1 block text-sm text-red-600">{form.errors.title}</span>}</label><label className="block"><span className="mb-2 block text-sm font-semibold text-slate-700">Description</span><textarea value={form.data.description} onChange={(e) => form.setData('description', e.target.value)} rows={5} className="w-full rounded-xl border border-slate-300 px-4 py-3" /></label><label className="block"><span className="mb-2 block text-sm font-semibold text-slate-700">Send to department</span><select value={form.data.target_department_id} onChange={(e) => form.setData('target_department_id', Number(e.target.value))} className="w-full rounded-xl border border-slate-300 px-4 py-3">{departments.map((d) => <option key={d.id} value={d.id}>{d.name}</option>)}</select>{form.errors.target_department_id && <span className="mt-1 block text-sm text-red-600">{form.errors.target_department_id}</span>}</label><label className="block"><span className="mb-2 block text-sm font-semibold text-slate-700">Routing remarks</span><textarea value={form.data.remarks} onChange={(e) => form.setData('remarks', e.target.value)} rows={3} className="w-full rounded-xl border border-slate-300 px-4 py-3" placeholder="Purpose or instructions for the receiving office" /></label><div className="flex justify-end gap-3"><Link href="/transactions" className="rounded-xl border border-slate-300 px-5 py-3 text-sm font-semibold text-slate-700">Cancel</Link><button disabled={form.processing} className="rounded-xl bg-[#0b2852] px-5 py-3 text-sm font-semibold text-white disabled:opacity-60">{form.processing ? 'Routing…' : 'Create & Route'}</button></div></form></div></AppLayout>;
}
